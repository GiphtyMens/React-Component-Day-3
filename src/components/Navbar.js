export const Navbar = () => {
  return <h1>My first Reat component</h1>;
};
// Name export:fixed cant be changed...its case sensitive...allows multiple export
// Default export:name not fixed, can only export one function in it
